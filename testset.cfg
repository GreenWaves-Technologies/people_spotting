from plptest import *

TestConfig = c = {}

test = Test(
  name = 'face_detection',
  commands = [
    Shell('clean', 'make clean'),
    Shell('build', 'make build'),
    Shell('run',   'make all run platform=gvsoc')
  ],
  timeout=1000000
)

c['tests'] = [ test ]
